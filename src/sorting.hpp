#include <vector>
#include "gtest/gtest.h"

namespace aalgo
{

// Take an array of ints and return that array sorted from least to greatest. Provably correct algorithm that's reasonably efficient, although there are more efficient sorting algorithms.
std::vector<int> insertionSort(std::vector<int> values);

} // aalgo

